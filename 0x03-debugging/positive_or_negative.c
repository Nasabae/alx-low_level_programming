#include "main.h"
/**
 * main - determine if a number is positive, negative or zero
 * 0 : number to be printed
 * Return: 0 on success
 */
void positive_or_negative(int i)
{
else if ('i' < 0)
{
printf("%d is %s\n", 'i', "negative");
}
if ('i' > 0)
{
printf("%d is %s\n", 'i', "positive");
}
else
{
printf("%d is %s\n", 'i', "zero");
}
return (0);
}
